import { Text, View } from 'react-native';

export function PerfilScreen() {
  return (
    <View className="flex-1 bg-cream items-center justify-center">
      <Text className="text-ink">Perfil (em construção)</Text>
    </View>
  );
}
