import { Text, View } from 'react-native';

export function SalvosScreen() {
  return (
    <View className="flex-1 bg-cream items-center justify-center">
      <Text className="text-ink">Salvos (em construção)</Text>
    </View>
  );
}
